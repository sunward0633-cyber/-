# OpenClash 界面配置清单

## 1. 导入前准备

- 确认 OpenWrt 已安装 OpenClash
- 优先使用支持 `GEOSITE` / `GEOIP` 的 Clash Meta / Mihomo 内核
- 将所选配置文件中的占位符替换为你的真实节点参数
- 如节点不是 Shadowsocks，请按你的协议改写 `proxies` 段

## 2. 导入配置

- 在 OpenClash 中上传并导入你选择的 YAML 配置文件
- 确认配置文件语法校验通过
- 选择刚导入的配置为当前运行配置

## 3. 运行模式建议

- 运行模式选择规则模式
- 使用支持规则集与地理数据库的内核
- 设备性能较弱时，优先采用较轻量配置，避免叠加过多自定义规则集

## 3.5 ⚠️ OpenClash 界面覆盖设置（关键）

OpenClash 界面中的部分开关会**覆盖**配置文件中的设定，即使 YAML 里写了 `enable: true` 也可能被界面关闭：

- **域名嗅探**：界面中必须选择"启用"，否则 `sniffer.enable: true` 不生效，导致部分流量无法正确分流
- **DNS 代理**：必须"启用"。若停用，DNS 查询不走 Clash 内部处理，fake-ip 模式不工作，可能导致 DNS 泄漏和额外延迟
- **区域绕过**：建议选择"大陆"，确保国内流量直连
- 每次更新或重导配置后，请务必重新确认以上界面设置是否被重置

## 4. DNS 核对

- 启用 Clash DNS
- 国内 DNS 使用阿里云 DNS / DNSPod 等国内解析
- 代理 DNS 使用 Cloudflare / Google 等可信国外解析
- 保持“国内域名国内解析、国外域名代理解析”的分流思路
- 如局域网设备依赖本地解析，确认 `.lan`、`.local` 等域名未被 fake-ip 干扰

## 5. 分流核对

- 大陆网站访问应命中 `DIRECT`
- 局域网与保留地址应命中 `DIRECT`
- 海外网站与需要代理的服务应命中 `国外流量`
- 若有流媒体、AI、游戏、BT 需求，可在 `MATCH` 规则之前补充专门规则

## 6. 节点组核对

- `节点选择`：用于手动切换主代理
- `自动选择`：用于测速与故障转移
- `国外流量`：默认承接所有非大陆流量
- `国内直连`：保留为直连策略组，便于后续细分

## 7. OpenWrt 场景补充建议

- 普通家宽环境优先确保 UDP 可用
- 若设备性能一般，可先关闭不必要的嗅探和复杂规则
- 若全屋设备共用，需确认网关模式、旁路由模式与 DHCP / DNS 指向一致
- 若出现国内网站走代理或海外网站直连异常，优先检查 DNS 与 GEO 数据库是否正常更新

## 8. 上线后验证

- 测试国内站点：百度、腾讯、阿里系服务应直连
- 测试海外站点：Google、YouTube、GitHub 海外资源应走代理
- 测试局域网访问：路由器管理页、NAS、打印机地址应直连
- 检查 OpenClash 连接日志，确认规则命中与预期一致

## 9. 延迟预期与排查

代理延迟 ≠ VPS ping 延迟。一次 HTTPS 请求经过代理的完整延迟大约为：

| 阶段                | 说明                           |
|---------------------|-------------------------------|
| 客户端 → VPS TCP    | 1 RTT（= ping 值）            |
| Reality/TLS 握手    | 1 RTT                         |
| VPS → 目标站 TCP    | 1 RTT（VPS 到目标通常 < 10ms）|
| VPS → 目标站 TLS    | 1-2 RTT                       |
| HTTP 请求 / 响应    | 1 RTT                         |

**冷连接总延迟 ≈ ping × 4 ~ 5**（首次请求），例如 ping 220ms 则首次请求约 880 ~ 1100ms。

**降低延迟的方法（按优先级排序）**：

1. **选择低延迟线路** — 最有效。将 VPS ping 从 220ms 降到 80ms，首次请求从 ~1000ms 降到 ~400ms
2. **确保 `keep-alive-interval` 已配置** — 复用已建立的连接，后续请求只需 1 RTT
3. **确保 `tcp-concurrent: true`** — 并发发起 TCP 连接，选最快的
4. **确保 `unified-delay: true`** — 测速时计入握手延迟，选出实际体验最优的节点
5. **在 OpenClash 界面启用"域名嗅探"和"DNS 代理"** — 避免 DNS 泄漏带来的额外延迟
